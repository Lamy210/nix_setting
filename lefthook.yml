pre-commit:
  parallel: true
  commands:
    format:
      run: nix fmt
    statix:
      glob: "*.nix"
      run: statix check {staged_files}
    deadnix:
      glob: "*.nix"
      run: deadnix --fail {staged_files}
