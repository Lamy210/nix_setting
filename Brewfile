# Homebrew dependencies for nix_setting
# Run: brew bundle --file=Brewfile

# Fonts (PlemolJP Console NF = Nerd Font variant with icons)
cask "font-plemol-jp-nf"
cask "font-jetbrains-mono-nerd-font"

# Terminal
cask "wezterm"

# Editor
cask "visual-studio-code"

# Window management (Phase 5 nix-darwin target)
cask "loop"
cask "flashspace"

# Clipboard manager (tap required)
tap "supergodak/tap"
cask "tameo"

# Monitor control (optional)
# cask "monitorcontrol"
