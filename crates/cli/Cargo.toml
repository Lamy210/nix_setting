[package]
name = "schneeforge"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "schneeforge"
path = "src/main.rs"

[dependencies]
schneeforge-core = { path = "../core" }
clap.workspace = true
serde.workspace = true
toml.workspace = true

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
libc = "0.2"
# test guard を binary と同一の tool 解決 (PATH 以外の profile 群も含む) に合わせるため
schneeforge-core = { path = "../core" }
